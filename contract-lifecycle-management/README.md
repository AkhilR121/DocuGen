


Features:
Proposed Tech Stack: react + rich text editor ( front-end ) AND python + fast-api AND oracle + redis

1. create a POC for contract document generation in three versions (a) docx, (b) html and (c) pdf
2. gather all types of contract documents
3. track the status of contract document in the database
4. edit the contract document and generate a new version if user requests edits ( backend )
5. provide contract document link so that users can edit in their web browsers ( like google-docs )
6. provide UI interface for contract document editing manually by users
7. e-signature for the contract document



Rich text editors: 
https://www.tiny.cloud/
https://tiptap.dev/
